image: docker:19.03.8

services:
  - docker:19.03.8-dind

stages:
  - Build php images

before_script:
  - docker info
  - apk update && apk add bash

docker build:
  stage: Build php images
  only:
    - schedules
  script:
    - ./scripts/build.sh --push
